.container
  %div.half
    %h2 And enter the rest of the metadata
    
    - form_for [:datasource, @datatable], :url => datasource_datatable_path(@datasource, @datatable), :html => { :class => "cmxform", :method => :put } do |f|
      = render :partial => "form", :locals => { :f => f }
      = f.submit "Update"
  
  %div.half{ :style => "overflow: scroll; height: 600px" }
    %h2 Datatable's Data
    
    %table
      %tr
      - @datatable.datacolumns.each do |datacol|
        %th= datacol.name

      - @datatable.rows.each do |row|
        %tr
          - row.each do |cell|
            %td= cell
