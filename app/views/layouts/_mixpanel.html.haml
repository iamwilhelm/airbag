:javascript
  var mp_protocol = (("https:" == document.location.protocol) ? "https://" : "http://");
  document.write(unescape("%3Cscript src='" + mp_protocol + "api.mixpanel.com/site_media/js/api/mixpanel.js' type='text/javascript'%3E%3C/script%3E"));
 
<!-- Initialize it with your project token -->
:javascript
  try { 
      var mpmetrics = new MixpanelLib("50cf3f4ca3b52c5af02f259c49cf94ce"); 
  } catch(err) {
      null_fn = function () {};
      var mpmetrics = { track: null_fn, track_funnel: null_fn, register: null_fn, register_once: null_fn };
  }
 
<!-- Then make calls to our API as shown below: -->
:javascript
  $(document).ready(function() {
    mpmetrics.track('Page view', { 'page': document.location });
  });
